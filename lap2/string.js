// # String trong Javascript tương tự có các hàm như trong Java (gần như đầy đủ)
// # Ngoài ra còn có một vài hàm khác như:
//     + slice(begin , end) -> tương tự subString()
//     + Có thể truy cập các kí tự như một mảng: str[i];
//     + search(str) -> tìm kiếm chuỗi trong chuỗi
//     + match(regex) -> tìm kiếm chuỗi theo regex
//     + matchAll(regex) -> tìm kiếm tất cả chuỗi theo regex 

let name = "Nguyen Van A       ";
console.log(name + 'k');
name = name.trim();
console.log(name + 'k');
console.log(name.toUpperCase());
console.log(name.toLowerCase());


